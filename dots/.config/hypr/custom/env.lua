-- Lua twin of custom/env.conf touch additions for Hyprland 0.55+.
-- Loaded by require("custom.env") from ~/.config/hypr/hyprland.lua.
--
-- Only contains the EDITOR env var uncommented on archspectre. Merge with
-- the upstream env.lua when you switch to Lua mode.

hl.env("EDITOR", "vim")
